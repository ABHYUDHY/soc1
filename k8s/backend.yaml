apiVersion: apps/v1
kind: Deployment
metadata: { name: soc-backend }
spec:
  replicas: 2
  selector: { matchLabels: { app: soc-backend } }
  template:
    metadata: { labels: { app: soc-backend } }
    spec:
      containers:
      - name: backend
        image: soc-backend:latest
        ports: [{ containerPort: 8000 }]
---
apiVersion: v1
kind: Service
metadata: { name: soc-backend }
spec:
  selector: { app: soc-backend }
  ports: [{ port: 8000, targetPort: 8000 }]
